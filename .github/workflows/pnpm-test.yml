name: Pnpm test
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
        with:
          version: 11.5.3
          runtime: node@24.15.0
          cache: true
          install: false
      - run: git diff
