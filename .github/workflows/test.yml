name: Test
on:
  push:
    branches: [ '🙄' ]

jobs:
  job:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Env
        run: env
      - name: Event data
        run: cat $GITHUB_EVENT_PATH
      - name: FOO
        env:
          FOO: ${{ secrets.FOO }}
        run: if [[ "$FOO" == "foobar" ]]; then echo "ok"; else echo "nope"; fi
