name: Test dispatch
on:
  push:

jobs:
  job:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Turnstile
        uses: ./.github/actions/turnstile
      - name: Work 
        run: sleep 120
